worker_processes  1;

events {
    worker_connections  1024;
}

http {
    server {
        include mime.types;
        listen 80;
        server_name  localhost;
        location / {
            proxy_pass http://front:3000/;
        }

        location /api/ {
            fastcgi_pass server:9001;
        }
    }
}